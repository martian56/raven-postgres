[package]
name = "raven-postgres"
version = "0.1.0"
edition = "v2"

# A pure-Raven PostgreSQL client: the v3 wire protocol over std/net (and
# std/tls for sslmode). No native dependencies; everything is stdlib.
[dependencies]
