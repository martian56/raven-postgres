[package]
name = "raven-postgres"
version = "0.1.0"
edition = "v2"

# A pure-Raven PostgreSQL client using the v3 wire protocol over std/net.
# No native dependencies; everything is standard-library Raven.
[dependencies]
